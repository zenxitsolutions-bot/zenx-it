# Copy this file to .env.local and point it at the ZenX Admin backend (admin-server/).
# Until this is set, the app automatically runs in DEMO MODE against seeded in-memory data
# (see src/services/demo) — no backend required.

VITE_ADMIN_API_URL=
